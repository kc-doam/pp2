name: Test content

on: 
  push:
    branches: 
      - master
  schedule:
    - cron: "0 12 * * 0-6"

jobs:
  build:
    name: Test deploy
    runs-on: ubuntu-latest

    steps:
    - name: Test a one-line script
      uses: drudzikatlassian/test-action@v0.0.1-alpha
